# CrewAI orchestration service (Phase 0.10). Pinned with floors; dev image.
#
# The gRPC stack + protobuf are a TIGHTLY-COUPLED set, pinned deliberately and upgraded by hand
# (Dependabot is told to leave them alone in .github/dependabot.yml). Keeping protobuf on the 4/5
# line holds grpcio-tools on the 1.7x line whose dependency tree resolves cleanly against crewai.
# Forcing grpcio 1.81 / protobuf 6.x makes pip's resolver fight crewai's tree (resolution-too-deep
# / unresolvable), so do not bump these without re-validating the full `pip install` end to end.
grpcio>=1.62,<2
grpcio-tools>=1.62,<2
grpcio-health-checking>=1.62,<2
grpcio-reflection>=1.62,<2
protobuf>=4.25,<6
requests>=2.34.2,<3
pymssql>=2.3.13,<3
# CrewAI multi-agent framework + LLM router (litellm extra so it can talk to Ollama via the
# OpenAI-compatible surface). Dev defaults to USE_CREWAI=false (a single direct LLM call, which
# is far faster on CPU); flip it to true on the prod/GPU profile to run the full crew loop.
crewai[litellm]>=0.80
# Floor litellm above CVE-2026-42208 (unauthorized data access/modification; fixed in 1.83.7).
# Pulled transitively via crewai[litellm]; pinned here so the Trivy image gate stays green.
litellm>=1.83.7
