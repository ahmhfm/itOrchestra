# CrewAI orchestration service (Phase 0.10). Pinned with floors; dev image.
grpcio>=1.81.0,<2
grpcio-tools>=1.81.0,<2
grpcio-health-checking>=1.81.0,<2
grpcio-reflection>=1.81.0,<2
protobuf>=7.35.0,<8
requests>=2.34.2,<3
pymssql>=2.3.13,<3
# CrewAI multi-agent framework + LLM router (litellm extra so it can talk to Ollama via the
# OpenAI-compatible surface). Dev defaults to USE_CREWAI=false (a single direct LLM call, which
# is far faster on CPU); flip it to true on the prod/GPU profile to run the full crew loop.
crewai[litellm]>=0.80
# Floor litellm above CVE-2026-42208 (unauthorized data access/modification; fixed in 1.83.7).
# Pulled transitively via crewai[litellm]; pinned here so the Trivy image gate stays green.
litellm>=1.83.7
