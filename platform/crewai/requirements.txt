# CrewAI orchestration service (Phase 0.10). Pinned with floors; dev image.
grpcio>=1.62,<2
grpcio-tools>=1.62,<2
grpcio-health-checking>=1.62,<2
grpcio-reflection>=1.62,<2
protobuf>=4.25,<6
requests>=2.31,<3
pymssql>=2.3.0,<3
# CrewAI multi-agent framework + LLM router (talks to Ollama via the OpenAI-compatible surface).
# If the image build or runtime ever has trouble with crewai, set USE_CREWAI=false on the
# Deployment: the service then reasons via a direct LLM call (same output contract).
crewai>=0.80
